# Configuration

This directory contains executable project configuration.

- `project.R` defines the frozen dates, coverage gates, bootstrap count, paths,
  and refresh behavior.

Reviewed analytical inputs live in `data/reference/`. Downloaded API responses
belong under `data/raw/` and are excluded from Git.
